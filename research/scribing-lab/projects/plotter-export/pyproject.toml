[project]
name = "plotter-export"
version = "0.1.0"
description = "Plotter export MVP for scribing lab trajectories"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
